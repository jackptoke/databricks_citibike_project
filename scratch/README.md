# scratch/

Throwaway exploration lives here. Everything in this folder is git-ignored
except this README, so notebooks and ad-hoc experiments never end up in the
repo. Promote anything worth keeping into `src/`.
